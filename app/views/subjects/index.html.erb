<% content_for :page_title do
  'Все курсы'
end %>
<div class="container">

<h1>Все курсы</h1>
<% if user_signed_in?&&current_user.admin? %>
<%= link_to 'Новый курс', new_admin_subject_path, class: 'btn btn-primary' %>
<% end %>
<br> 
<!-- 
<table>
  <tr>
  	<th>id</th>
    <th>Тема</th>
    <th>Описание</th>
    <th>updated at</th>
  </tr>
 
  <% @subjects.each do |subject| %>
    <tr>
    	<td><%= subject.id %></td>
      <td><%= subject.title %></td>
      <td><%= truncate(subject.description, length: 17) %></td>
      <td><%= subject.updated_at %></td>
      <td><%= link_to 'Показать', subject_path(subject) %></td>
      <td><%= link_to 'Редактировать', edit_admin_subject_path(subject) %></td>
      <td><%= link_to 'Удалить', admin_subject_path(subject), method: :delete,data: {confirm: 'Вы уверены?'} %></td>
    </tr>
  <% end %>
</table>
 -->
 <% @subjects.each do |subject| %>
  <br>
  <div class="row">
    <div class="col-4">
      <h2><%= link_to subject.title, subject_path(subject) %></h2>
    </div>  
    <div class="col-8">
      
      <p class="text-muted"><%= subject.created_at.strftime("%d.%m.%Y"+" в "+"%H:%M") %> </p>
      <%= truncate(subject.description, length: 147) %>
    </div>
  </div>
  
  <hr> 
  <% end %>
</div>